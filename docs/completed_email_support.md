# Добавлена поддержка Email в сценарии нового клиента

**Дата:** 08 ноября 2025
**Время:** 01:15 - 01:25

---

## Выполнено

### ✅ Добавлен параметр email во все функции

**Обновленные функции:**

1. **`create_lead()`** - добавлен параметр `email: str = ""`
2. **`create_amocrm_lead()`** - добавлен параметр `email: str = ""`
3. **FUNCTIONS definition** - добавлен параметр email в описание для AI

---

## Изменения в коде

### 1. Сигнатура функции create_lead

**Было:**
```python
async def create_lead(
    name: str,
    phone: str,
    address: str,
    comment: str = "",
    city: str = "",
    tariff: str = "",
    router_option: str = "",
    cctv_option: str = "",
    static_ip: str = "",
    preferred_date: str = "",
    preferred_time: str = ""
) -> Dict[str, Any]:
```

**Стало:**
```python
async def create_lead(
    name: str,
    phone: str,
    address: str,
    comment: str = "",
    city: str = "",
    tariff: str = "",
    router_option: str = "",
    cctv_option: str = "",
    static_ip: str = "",
    preferred_date: str = "",
    preferred_time: str = "",
    email: str = ""  # ← Новый параметр
) -> Dict[str, Any]:
```

---

### 2. Email в AmoCRM контакте

**Добавлено в `create_amocrm_lead()`:**
```python
# Добавляем email если указан
if email:
    contact_custom_fields.append({
        "field_code": "EMAIL",
        "values": [{"value": email, "enum_code": "WORK"}]
    })
```

**Результат:**
- Email сохраняется в контакте AmoCRM с типом "Рабочий"
- Поле стандартное AmoCRM, не требует дополнительной настройки

---

### 3. Email в FreeScout

#### 3.1. В сообщении тикета

**Добавлено в формирование message:**
```python
message += f"👤 Контакт: {name}\n"
message += f"📞 Телефон: {phone}\n"
if email:
    message += f"📧 Email: {email}\n"  # ← Новая строка
message += f"📍 Адрес: {address}\n"
```

#### 3.2. В customer.email

**Было:**
```python
customer_email=f"{phone.replace('+', '').replace(' ', '')}@customer.local"
```

**Стало:**
```python
customer_email=email if email else f"{phone.replace('+', '').replace(' ', '')}@customer.local"
```

**Результат:**
- Если email указан → используется реальный email
- Если email НЕ указан → используется {phone}@customer.local

---

### 4. Описание для AI ассистента (FUNCTIONS)

**Добавлено в parameters:**
```json
{
    "email": {
        "type": "string",
        "description": "Email клиента (опционально, но рекомендуется спросить)"
    }
}
```

**Required поля:** `["name", "phone", "address", "city", "tariff", "preferred_date", "preferred_time"]`

Email опциональный, но AI рекомендуется его запрашивать.

---

## Тестирование

### Тест #64

**Входные данные:**
```
Имя: Алексей Новиков
Телефон: +79003334455
Email: alexey.novikov@example.com
Адрес: Волгоград, ул. Пушкина, д. 20, кв. 15
Город: Волгоград
Тариф: Smit — 70 Мбит/с за 840 руб/мес
```

**Результаты:**

✅ **FreeScout Ticket #64:**
- Customer Email: `alexey.novikov@example.com`
- В сообщении: `📧 Email: alexey.novikov@example.com`
- URL: https://support.smit34.ru/conversation/64

✅ **AmoCRM Contact 75005749:**
- Email: `alexey.novikov@example.com`
- Тип: Рабочий (WORK)
- URL: https://pavelsmit34ru.amocrm.ru/contacts/detail/75005749

✅ **AmoCRM Lead 45927747:**
- Контакт привязан с email
- Статус: "Тариф выбран" (79103554)
- URL: https://pavelsmit34ru.amocrm.ru/leads/detail/45927747

---

## Поведение при отсутствии email

Если клиент не указал email, система работает как раньше:

**Пример без email:**
```python
create_lead(
    name="Иван Петров",
    phone="+79001234567",
    address="Волгоград, ул. Мира, 1",
    city="Волгоград",
    tariff="Smit — 70 Мбит/с",
    email=""  # Пустой
)
```

**Результат:**
- FreeScout customer email: `79001234567@customer.local`
- AmoCRM контакт: БЕЗ поля email
- Сообщение тикета: БЕЗ строки 📧 Email

---

## Сценарий диалога с новым клиентом (обновленный)

### Последовательность сбора данных:

1. ✅ **Проверка адреса** → `check_address_gas(address)`
2. ✅ **Показ тарифов** → `get_tariffs_gas()`
3. ✅ **Выбор тарифа** → клиент выбирает
4. ✅ **Дополнительные услуги:**
   - Роутер (если нужен) → `offer_router()`
   - Постоянный IP → `offer_static_ip()`
   - Видеонаблюдение (для домов) → `offer_cctv()`
5. ✅ **Имя клиента** → "Как вас зовут?"
6. ✅ **Телефон** → "Укажите номер телефона"
7. ✅ **Email** → "Укажите ваш email для связи" ← **НОВОЕ**
8. ✅ **Уточнение адреса** → квартира/дом (если нужно)
9. ✅ **Дата и время визита** → "Когда удобно приехать мастеру?"
10. ✅ **Создание заявки** → `create_lead(...)`

---

## Примеры вопросов для запроса email

### Вариант 1 (рекомендуемый):
```
Отлично! Укажите ваш email для связи.
Это поможет вам получать уведомления о статусе заявки.
```

### Вариант 2:
```
Для оперативной связи укажите, пожалуйста, ваш email.
```

### Вариант 3:
```
Есть ли у вас email адрес? Можем отправить туда подтверждение заявки.
```

### Если клиент отказывается:
```
Клиент: У меня нет email / Не хочу указывать

Ассистент: Хорошо, можем обойтись без него.
           [Продолжаем без email, переходим к следующему шагу]
```

---

## Файлы изменений

### Скрипты обновления:
1. `add_email_support.py` - основное обновление кода
2. `update_create_lead_function_v2.py` - обновление FUNCTIONS
3. `test_email_support.py` - тест с email

### Измененные файлы на сервере:
- `/var/www/aida-gpt/server.py` - обновлены функции и FUNCTIONS

---

## API Endpoints

### AmoCRM - Добавление email в контакт

```http
POST https://pavelsmit34ru.amocrm.ru/api/v4/contacts
Headers:
  Authorization: Bearer {token}
  Content-Type: application/json

Body:
[{
  "name": "Алексей Новиков",
  "custom_fields_values": [
    {
      "field_code": "PHONE",
      "values": [{"value": "+79003334455", "enum_code": "WORK"}]
    },
    {
      "field_code": "EMAIL",
      "values": [{"value": "alexey.novikov@example.com", "enum_code": "WORK"}]
    }
  ]
}]
```

### FreeScout - Создание conversation с email

```http
POST https://support.smit34.ru/api/conversations
Headers:
  X-FreeScout-API-Key: {api_key}
  Content-Type: application/json

Body:
{
  "customer": {
    "email": "alexey.novikov@example.com",  // Реальный email
    "firstName": "Алексей Новиков",
    "phone": "+79003334455"
  },
  "threads": [{
    "text": "...\n📧 Email: alexey.novikov@example.com\n..."
  }]
}
```

---

## Команды для тестирования

### Тест с email:
```bash
ssh root@31.44.7.144 "cd /var/www/aida-gpt && python3 /tmp/test_email_support.py"
```

### Проверка email в FreeScout:
```bash
curl -s 'https://support.smit34.ru/api/conversations/64?embed=customer' \
  -H 'X-FreeScout-API-Key: dd2f001033f31ab74caa0db5c6f75553' \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print('Email:', d['customer']['email'])"
```

### Проверка сообщения:
```bash
curl -s 'https://support.smit34.ru/api/conversations/64?embed=threads' \
  -H 'X-FreeScout-API-Key: dd2f001033f31ab74caa0db5c6f75553' \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['_embedded']['threads'][0]['body'])" \
  | grep Email
```

---

## Статус интеграций

### ✅ Полностью работающие функции:

1. **Отображение роутера в подарок** - 5 тарифов показывают 🎁
2. **Email в FreeScout** - сохраняется как customer.email
3. **Email в AmoCRM** - сохраняется в контакте
4. **Email в сообщении** - отображается с 📧 эмодзи
5. **Перекрестные ссылки FreeScout → AmoCRM** - в сообщении и профиле
6. **Заполнение профиля FreeScout** - website, city, address, state

### ⚠️ Требует уточнения:

1. **AmoCRM → FreeScout** - поле HelpDesk ID (2563557) не найдено

---

## Следующие шаги

### 1. Обновить системный промпт (рекомендуется)
Добавить в "Сценарий 2: Новый клиент" запрос email после телефона:

```
7. Запросить имя клиента
8. Запросить номер телефона → подтвердить
9. Запросить email → "Укажите ваш email для связи" (опционально)
10. Уточнить тип помещения (если неясно из адреса)
11. Запросить дату и время визита
12. Создать заявку → create_lead(...)
```

### 2. Валидация email (опционально)
Добавить проверку формата email перед сохранением.

### 3. Автоматическая отправка подтверждения на email (будущее)
После создания заявки отправлять клиенту email с деталями.

---

## Текущее состояние системы

**Версия:** 08.11.2025 01:25
**Сервисы:** ✅ Running
- aida-gpt.service (port 8900)
- aida-cache-bot.service

**Тестовые данные:**
- Последний успешный тест: #64
- Email: alexey.novikov@example.com
- AmoCRM Contact: 75005749
- AmoCRM Lead: 45927747

---

**Конец документа**
