# Синхронизация FreeScout → AmoCRM - Завершено

**Дата:** 08 ноября 2025
**Время:** 03:20

---

## ✅ Реализовано

### Backend AIDA GPT (порт 8900)

**Endpoint:** `POST http://31.44.7.144:8900/freescout/webhook`

**Функции:**
1. ✅ `find_amocrm_contact_by_phone()` - поиск контакта в AmoCRM по номеру телефона
2. ✅ `add_note_to_amocrm_contact()` - добавление примечаний в AmoCRM
3. ✅ `handle_freescout_ticket_created()` - обработка создания тикета
4. ✅ `handle_freescout_reply_created()` - обработка ответов (клиент/агент)
5. ✅ `handle_freescout_ticket_closed()` - обработка закрытия тикета

---

### Frontend FreeScout

**Модуль:** AmoCRMSync
**Путь:** `/var/www/freescout/Modules/AmoCRMSync/`
**Статус:** ✅ Активирован

**Отслеживаемые события:**
- `conversation.created` - создание нового тикета
- `conversation.status_changed` - изменение статуса (включая закрытие)
- `conversation.customer_replied` - ответ клиента
- `thread.created` (TYPE_MESSAGE) - ответ агента

---

## 🔄 Как это работает

### 1. Создание тикета в FreeScout

**Событие:** `conversation.created`

**Что происходит:**
1. Модуль AmoCRMSync перехватывает событие
2. Отправляет webhook в AIDA GPT с данными:
   ```json
   {
     "event": "conversation.created",
     "conversation": {
       "id": 65,
       "number": 65,
       "subject": "Подключение интернета",
       "status": 1
     },
     "customer": {
       "id": 31,
       "first_name": "Бамба",
       "phones": ["+79371944417"]
     }
   }
   ```
3. AIDA GPT ищет контакт в AmoCRM по телефону
4. Добавляет примечание:
   ```
   📩 Создан новый тикет #65
   Тема: Подключение интернета
   Ссылка: https://support.smit34.ru/conversation/65
   ```

**Результат в AmoCRM:**
- В профиле клиента появляется примечание
- Есть прямая ссылка на тикет FreeScout

---

### 2. Ответ в тикете (клиент или агент)

**События:** `conversation.customer_replied`, `thread.created`

**Что происходит:**
1. Модуль перехватывает ответ
2. Определяет, кто ответил (клиент или агент)
3. Отправляет данные в AIDA GPT:
   ```json
   {
     "event": "conversation.agent_replied",
     "conversation": {"number": 65},
     "thread": {
       "body": "<p>Здравствуйте!</p>",
       "created_by": {
         "first_name": "Александр",
         "last_name": "Успешный"
       }
     }
   }
   ```
4. AIDA GPT создает примечание:
   ```
   💬 Новый ответ в тикете #65
   От: Александр Успешный
   Сообщение: Здравствуйте! Подключение возможно...
   Ссылка: https://support.smit34.ru/conversation/65
   ```

**Результат в AmoCRM:**
- Видно, кто ответил (имя агента или "Клиент")
- Превью сообщения (до 200 символов)
- Ссылка на тикет

---

### 3. Закрытие тикета

**Событие:** `conversation.status_changed` (status = 3)

**Что происходит:**
1. При смене статуса на "Closed" (3)
2. Отправляется webhook
3. Создается примечание:
   ```
   ✅ Тикет #65 закрыт
   Тема: Подключение интернета
   Ссылка: https://support.smit34.ru/conversation/65
   ```

**Результат в AmoCRM:**
- Фиксируется факт закрытия тикета
- Вся история доступна в одном месте

---

## 📊 Архитектура

```
┌──────────────────────┐
│  FreeScout Events    │
│                      │
│  - Создание тикета   │
│  - Ответ клиента     │
│  - Ответ агента      │
│  - Закрытие тикета   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────┐
│  AmoCRMSync Module       │
│  (FreeScout)             │
│                          │
│  Eventy::addAction()     │
│  - conversation.created  │
│  - thread.created        │
│  - status_changed        │
└──────────┬───────────────┘
           │ HTTP POST
           │ JSON payload
           ▼
┌──────────────────────────┐
│  AIDA GPT Webhook        │
│  :8900/freescout/webhook │
│                          │
│  1. Parse event          │
│  2. Find contact by phone│
│  3. Add note to AmoCRM   │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────┐
│  AmoCRM API v4           │
│                          │
│  GET /contacts?query=... │
│  POST /contacts/{id}/notes│
└──────────────────────────┘
```

---

## 🧪 Тестирование

### Тест 1: Создание тикета

**Шаги:**
1. Создай новый тикет в FreeScout от клиента с телефоном +79371944417
2. Открой профиль в AmoCRM: https://pavelsmit34ru.amocrm.ru/contacts/detail/75005811
3. Проверь примечания

**Ожидаемый результат:**
```
📩 Создан новый тикет #<номер>
Тема: <тема тикета>
Ссылка: https://support.smit34.ru/conversation/<номер>
```

---

### Тест 2: Ответ агента

**Шаги:**
1. Добавь ответ в тикет от агента
2. Обнови профиль AmoCRM

**Ожидаемый результат:**
```
💬 Новый ответ в тикете #<номер>
От: <Имя> <Фамилия>
Сообщение: <текст сообщения>
Ссылка: https://support.smit34.ru/conversation/<номер>
```

---

### Тест 3: Закрытие тикета

**Шаги:**
1. Закрой тикет в FreeScout
2. Обнови профиль AmoCRM

**Ожидаемый результат:**
```
✅ Тикет #<номер> закрыт
Тема: <тема>
Ссылка: https://support.smit34.ru/conversation/<номер>
```

---

### Тест прямого вызова webhook

```bash
python3 /tmp/test_webhook.py
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "contact_id": 75005811,
  "conversation_number": 999
}
```

---

## 🔍 Логи и отладка

### Логи AIDA GPT
```bash
tail -f /var/www/aida-gpt/logs/server.log | grep -i 'webhook\|freescout'
```

**Примеры записей:**
```
📨 FreeScout webhook: conversation.created
✅ Найден контакт AmoCRM: 75005811 для телефона +79371944417
✅ Примечание добавлено к контакту 75005811
```

---

### Логи FreeScout
```bash
tail -f /var/www/freescout/storage/logs/laravel.log | grep -i amocrm
```

**Примеры записей:**
```
AmoCRMSync: Event conversation.created sent successfully for conversation #65
```

---

### Проверка состояния модуля
```bash
cd /var/www/freescout
php artisan module:list | grep AmoCRM
```

**Ожидаемый вывод:**
```
| AmoCRMSync           | Enabled  | 100   | /var/www/freescout/Modules/AmoCRMSync |
```

---

## ⚙️ Конфигурация

### AIDA GPT (.env)
```bash
AMO_BASE_URL=https://pavelsmit34ru.amocrm.ru
AMO_ACCESS_TOKEN=<токен>
FREESCOUT_URL=https://support.smit34.ru
```

### AmoCRMSync Module
```php
define('AIDA_WEBHOOK_URL', 'http://31.44.7.144:8900/freescout/webhook');
```

---

## 🔧 Управление модулем

### Деактивация
```bash
cd /var/www/freescout
php artisan module:disable AmoCRMSync
php artisan cache:clear
```

### Активация
```bash
cd /var/www/freescout
php artisan module:enable AmoCRMSync
php artisan cache:clear
```

### Очистка кеша
```bash
cd /var/www/freescout
php artisan cache:clear
php artisan view:clear
php artisan config:clear
```

---

## 📝 Структура примечаний AmoCRM

### API Endpoint
```
POST https://pavelsmit34ru.amocrm.ru/api/v4/contacts/{contact_id}/notes
```

### Headers
```
Authorization: Bearer <token>
Content-Type: application/json
```

### Payload
```json
[
  {
    "note_type": "common",
    "params": {
      "text": "📩 Создан новый тикет #65\nТема: Подключение\nСсылка: https://..."
    }
  }
]
```

### Типы примечаний
- `common` - обычное текстовое примечание (используется)
- `call_in` - входящий звонок
- `call_out` - исходящий звонок
- `sms_in` - входящее SMS
- `sms_out` - исходящее SMS

---

## 🎯 Преимущества

1. **Полная история взаимодействия** - все события FreeScout в одном профиле AmoCRM
2. **Кликабельные ссылки** - прямой переход к тикету из AmoCRM
3. **Имена агентов** - видно, кто отвечал клиенту
4. **Автоматическая связка** - поиск по телефону без ручной работы
5. **Независимая работа** - даже если AIDA GPT недоступен, FreeScout работает

---

## 🚀 Статус

**Версия:** AIDA GPT v2.3 с FreeScout синхронизацией
**Дата:** 08.11.2025 03:20
**Модули:** ✅ Все активированы

**Компоненты:**
- ✅ AIDA GPT webhook endpoint
- ✅ FreeScout модуль AmoCRMSync
- ✅ Интеграция с AmoCRM notes API
- ✅ Поиск контактов по телефону
- ✅ Отображение имен агентов

**Тестирование:**
- ✅ Прямой вызов webhook - работает
- ✅ Поиск контакта - работает
- ✅ Добавление примечания - работает

---

**Готово к использованию!**

**Конец документа**
